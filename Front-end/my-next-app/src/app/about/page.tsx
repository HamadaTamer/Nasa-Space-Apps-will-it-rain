// src/app/about/page.tsx

import React from 'react';
import AboutPageContent from '../../components/core/AboutPageContent';

/**
 * Renders the About page, detailing the project's purpose, methodology, 
 * and value proposition based on the hackathon proposal document.
 */
export default function AboutPage() {
    return (
        <AboutPageContent />
    );
}